# Tor Sustainability

## Data Provided

### Network Samples

We provide the five, 10% scaled, Tor network samples used for our Shadow simulations, each named `n1` through `n5`. Each network directory contains the cache files from the simulation results and the corresponding Shadow configuration files.

For each network, we provide cached results for 24 hours of simulation across six configurations: one baseline and five carbon-aware configurations.

The Shadow configuration files are named `shadow.config.yaml#`, where `#` is an hour of the day (1–23). `shadow.config.yaml` (no suffix) is the original configuration file generated by Tornettools; the hourly files are derived from it and control the simulated workload for that hour.

We also provide the Jupyter notebooks used to analyze the provided data and generate the figures in our paper, covering both the simulation results and the open relay data.

### Open Relay Data

We provide data collected from our three open relays. For safety, we release only daily aggregate data, found under `france/`, `germany/`, and `great-britain/`.

## Missing Data

We do not release the raw Shadow simulation data, primarily due to its size — the 24-hour dataset for a single configuration alone is approximately 450GB. Instead, we provide the cached data files needed to reproduce the figures in our paper.

We also do not release per-second data from the open relays, for security reasons.

## Repository Structure

```
tor-sustainability-anon/
├── n1/ ... n5/                        # Network samples
│   ├── n#-configs/                    # Shadow configuration files
│   └── n#-results.zip                 # Cached simulation results
│
├── networks.tar.gz                    # Combined network samples archive
│
├── france.tar.gz                      # Open relay daily aggregates
├── germany.tar.gz
├── great-britain.tar.gz
│
├── section3_open_relay_power_curves.ipynb
└── section5_carbon_optimization.ipynb
```
